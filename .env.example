# Environment Configuration for Vercel Deployment

# Node Environment
NODE_ENV=production

# Vercel automatically provides these variables:
# VERCEL_URL=your-app-name.vercel.app
# VERCEL_ENV=production

# Optional: Custom domain (if you have one)
# CUSTOM_DOMAIN=your-custom-domain.com

# Socket.IO Configuration
# These are handled automatically, but you can override if needed
# SOCKET_IO_ORIGINS=https://your-app-name.vercel.app

# For local development, create a .env.local file with:
# NODE_ENV=development
